# Batch 33 — Authoring Workflow Controls

Compatibility Lab now exposes topology reset and deform-key preview controls alongside weight painting and heatmap controls. Each action uses the Phase 6 authoring state APIs and reports its resulting state without mutating imported fixtures.
