# Batch 71: Add vertex topology command

- Added `addVertex` with stable unique ID validation.
- Existing triangles and explicit edges are preserved when adding an isolated vertex.
- Validation: `pnpm check`.
