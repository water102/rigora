# Batch 70: Linked deform UI

- Added an inherit-linked-deform toggle and apply action.
- The action uses the shared domain-safe inherit API and persists the resulting offsets.
- Validation: `pnpm check`.
