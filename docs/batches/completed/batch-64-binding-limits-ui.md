# Batch 64: Binding limits UI

- Added lock-bone toggle, maximum-influences input, Apply limits, and List influences controls.
- UI uses the shared sparse weight APIs and persists successful limit operations.
- Validation: `pnpm check`.
