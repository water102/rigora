# Batch 42 — Canvas Weight Brush

Adds a real canvas `Weight brush` mode. Pointer down/move applies smoothstep radius falloff to nearby stable vertices, reports sparse delta counts during preview, and commits the stroke on pointer release.
