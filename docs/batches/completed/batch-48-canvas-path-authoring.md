# Batch 48 — Canvas Path Authoring

Adds a Path control-point canvas mode. Dragging the first control point updates the editable path, previews its tangent, commits on release, and persists the path in the authoring document.
