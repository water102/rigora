# Batch 31 — Auto-Mesh Worker Preview Integration

The Compatibility Lab now exposes alpha threshold controls and preview/cancel actions backed by the Comlink mesh worker. The preview path is intentionally non-mutating; applying a result remains an authoring command concern.
