# Batch 20: Stage Framing Utilities

Adds renderer-independent bounds calculation and `framePoints()` to `@rigora/editor-core`. Stage code can frame all visible geometry or only the current selection using the same validated point set and camera behavior.
