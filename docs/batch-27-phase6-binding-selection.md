# Batch 27 — Phase 6 Binding and Selection Utilities

Completes the binding/heatmap/selection utility layer used by the Phase 6 UI. Binding and unbinding return sparse deltas suitable for command history; heatmap values are clamped and selection helpers operate on mesh adjacency.
