# Batch 55: Auto weights preview UI

- Added configurable auto-weight exponent and maximum influence controls.
- Preview runs in the mesh worker using inverse distance to demo bone segments.
- The lab reports vertex and influence counts before any authored state is changed.
- Validation: `pnpm check`.
