# Batch 14: Native `.hbone` Project Persistence

Adds `@rigora/project`, a deterministic ZIP-compatible native project envelope containing a manifest, validated skeleton snapshots, and optional editor state. Project parsing validates the manifest and every skeleton before returning data.
