# Batch 34 — Lasso and Soft Brush

Adds exact polygon lasso selection and radius-aware brush strokes with linear, smoothstep, and Gaussian falloff. Brush operations continue to return sparse deltas and respect normalized weights through the existing authoring API.
