# Batch 26 — Phase 6 Authoring Core

Adds renderer-independent mesh topology editing, sparse reversible weight brush deltas, polygon/connected/invert selection, binding helpers, heatmap sampling, and editable path points. The APIs are deterministic and preserve stable vertex IDs across position edits.
