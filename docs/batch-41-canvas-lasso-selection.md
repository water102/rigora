# Batch 41 — Canvas Lasso Selection

Compatibility Lab now supports selectable canvas modes: vertex drag and pointer-path lasso selection. Lasso gestures report selected stable vertex IDs, and pointer cancellation clears both gesture states without committing changes.
