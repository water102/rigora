# Batch 17: Framework-Independent Selection Model

Adds `SelectionStore` to `@rigora/editor-core` for selecting bones, slots, attachments, constraints, and animations. Selection is UI state, not authored state, and therefore does not enter command history.
