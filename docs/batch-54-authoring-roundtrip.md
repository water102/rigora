# Batch 54: Authoring round-trip

- Compatibility Lab load now restores mesh, editable path points/closed state, and demo deform offsets.
- Restored data is cloned before entering live authoring state and deform offsets are applied only when the vertex domain matches.
- Validation: `pnpm check`.
