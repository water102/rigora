# Batch 29 — Topology Commands

Adds deterministic topology commands for edge insertion/removal and topology reset while preserving vertex IDs and copied positions. These operations are suitable for command-history integration and connected-selection adjacency rebuilds.
