# Batch 73: Brush replace mode

- Added Replace to the Compatibility Lab brush mode selector.
- Replace uses the existing sparse brush delta path and remains compatible with radius, falloff, bone, and locks.
- Validation: `pnpm check`.
