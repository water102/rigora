# Batch 45 — Canvas Brush Undo/Redo

Canvas brush strokes are now collected as sparse delta commands with dedicated Undo/Redo controls. A complete pointer stroke is one reversible command, including all sampled pointer positions.
