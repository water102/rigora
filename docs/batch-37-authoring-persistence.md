# Batch 37 — Authoring State Persistence

Adds a versioned authoring document contract with deterministic JSON serialization, validation, mesh/path persistence helpers, and safe round-trip cloning for embedding in the native project `editorState`/extensions payload.
